---

---

<header>
  <div id="header-wrapper">
    <a href="/" id="logo-wrapper">
      <img id="logo" src="/otrocadev-logo.svg" alt="Otrocadev's logo" />
      <h2>otroca.dev</h2>
    </a>
    <nav id="nav-menu">
      <ul id="menu-elements">
        <li>
          <a class="menu-element" href="#projects">Projects</a>
        </li>
        <li>
          <a class="menu-element" href="#experience">Experience</a>
        </li>
        <li>
          <a class="menu-element" href="#education">Education</a>
        </li>
        <li>
          <a class="menu-element" href="#contact">Contact</a>
        </li>
      </ul>
    </nav>
  </div>
</header>

<style>
  h2 {
    font-size: clamp(20px, 2vw, 48px);
    margin: 0 1rem;
  }

  header {
    width: 100%;
    height: 5vh;
    min-height: 60px;
    position: fixed;
    display: flex;
    color: var(--screaming-green);
    backdrop-filter: blur(8px);
    box-sizing: border-box;
    z-index: 2;
    border-bottom: 2px solid var(--background-lines);
    max-width: 3840px;
  }

  #header-wrapper {
    margin: auto;
    height: 100%;
    width: 100%;
    padding: 0 2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  #logo-wrapper {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
  }

  #logo-wrapper:hover,
  #logo-wrapper:active {
    transform: scale(1.05);
  }

  #logo {
    height: 36px;
  }

  #nav-menu {
    display: none;
    @media (min-width: 720px) {
      display: flex;
    }
  }

  #menu-elements {
    display: flex;
    list-style: none;
    gap: 2rem;
    font-size: clamp(20px, 2vw, 48px);
    font-weight: 700;
  }

  .menu-element {
    color: var(--screaming-green);
    border-radius: 0;
    border-bottom: 2px transparent solid;
    transition: ease 300ms;
  }

  .menu-element:hover,
  .menu-element:active {
    border-bottom: 2px var(--screaming-green) solid;
  }
</style>
